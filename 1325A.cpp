#include "1325A.h"

void _1325A()
{
    long t, x;
    std::cin >> t;

    for (int i = 0; i < t; ++i)
    {
        std::cin >> x;
        std::cout << std::endl << 1 << std::endl << x - 1 << std::endl;
    }
}